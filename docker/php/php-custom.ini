memory_limit=512M

; Allow agents to relay arbitrarily large backup archives to the server.
; The upload endpoint streams the raw request body to disk, so this does not
; affect memory usage. 0 = no PHP-level limit (Caddy/FrankenPHP still applies
; its own request body handling).
post_max_size=0
